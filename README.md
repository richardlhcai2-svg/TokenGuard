# TokenGuard

AI development cost intelligence platform — sleep well while your team uses AI.

## Quick Start

```bash
docker compose up -d
```

Services:
- Backend API: http://localhost:8000
- Proxy: http://localhost:8001
- Frontend: http://localhost:3000

## Stack

Next.js 14 + FastAPI + PostgreSQL + TimescaleDB + Redis + Celery
