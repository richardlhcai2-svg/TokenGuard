[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tokenguard-cli"
version = "0.1.0"
description = "TokenGuard CLI — AI development cost intelligence"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
tg = "tokenguard_cli.cli:main"

[tool.setuptools.packages.find]
include = ["tokenguard_cli*"]
